Lecture 8 Submission Notes

This submission uses the AWS option from the lecture 8 assignment.

Included work:
- Terraform root files for baseline infrastructure
- A reusable S3 bucket module
- `for_each` for multiple storage buckets
- A DynamoDB table used as the database equivalent for pipeline metadata

Submission reminder:
- Capture one screenshot of the deployed infrastructure or AWS console
- Capture one screenshot of `terraform output`
- Keep the `modules/` folder in the PR
